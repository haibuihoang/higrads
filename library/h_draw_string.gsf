*
*  Draw a string at a specific location on the charts,
*  vx,vy is the chart coordinates, not drawing coordinates
*
function h_draw_string(vx,vy,string)

   tempx = (_px2-_px1)/(_xmax-_xmin)
   tempy = (_py2-_py1)/(_ymax-_ymin)

   x = _px1 + (vx-_xmin)*tempx
   y = _py1 + (vy-_ymin)*tempy
   'draw string 'x' 'y' 'string

return
