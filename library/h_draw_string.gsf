*
*  Draw a string at a specific location on the charts,
*  vx,vy is the chart coordinates, not drawing coordinates
*
function h_draw_string(vx,vy,string,offset_x,offset_y)
   if (offset_x='offset_x'); offset_x=0; endif
   if (offset_y='offset_y'); offset_y=0; endif
   x = h_get_px(vx) + offset_x
   y = h_get_py(vy) + offset_y
   'draw string 'x' 'y' 'string

return
